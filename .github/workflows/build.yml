name: Build JYJH AntiDetect Dylib (v3.0)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq git cmake ninja-build python3 \
            libssl-dev zlib1g-dev libxml2-dev uuid-dev libbsd-dev \
            autoconf automake libtool pkg-config clang libc++-dev libc++abi-dev xz-utils

      - name: Get iOS SDK
        run: |
          git clone https://github.com/theos/sdks.git --depth 1 /tmp/sdks
          cd /tmp/sdks
          tar -cJf /tmp/iPhoneOS15.6.sdk.tar.xz iPhoneOS15.6.sdk
          ls -lh /tmp/iPhoneOS15.6.sdk.tar.xz

      - name: Build cctools-port toolchain
        run: |
          git clone https://github.com/tpoechtrager/cctools-port.git --depth 1 /tmp/cctools
          cd /tmp/cctools/usage_examples/ios_toolchain
          ./build.sh /tmp/iPhoneOS15.6.sdk.tar.xz arm64

      - name: Build AntiDetect v3.0 dylib (no Dobby needed)
        run: |
          TOOLCHAIN=/tmp/cctools/usage_examples/ios_toolchain/target
          export PATH="$TOOLCHAIN/bin:$PATH"
          SDK="$TOOLCHAIN/SDK/iPhoneOS15.6.sdk"
          
          echo "=== Build Environment ==="
          echo "SDK: $SDK"
          which arm-apple-darwin11-clang || { echo "ERROR: clang not found!"; ls "$TOOLCHAIN/bin/" | head -20; exit 1; }
          echo "=== Source file ==="
          ls -la JYJH_AntiDetect_v3.m
          head -5 JYJH_AntiDetect_v3.m
          
          echo "=== Compiling ==="
          arm-apple-darwin11-clang \
            -arch arm64 \
            -miphoneos-version-min=15.0 \
            -isysroot "$SDK" \
            -fobjc-arc \
            -I. \
            -c JYJH_AntiDetect_v3.m \
            -o JYJH_AntiDetect.o
          
          echo "Compile OK, linking (no Dobby)..."
          
          arm-apple-darwin11-ld \
            -arch arm64 \
            -iphoneos_version_min 15.0 \
            -syslibroot "$SDK" \
            -dynamic \
            -dylib \
            -framework Foundation \
            -framework UIKit \
            -framework CoreGraphics \
            -framework CoreFoundation \
            -lSystem \
            -lobjc \
            -lc++ \
            -objc_abi_version 2 \
            -install_name @executable_path/JYJH_AntiDetect.dylib \
            -o JYJH_AntiDetect.dylib \
            JYJH_AntiDetect.o
          
          echo "Build SUCCESS!"
          
          file JYJH_AntiDetect.dylib
          ls -lh JYJH_AntiDetect.dylib

      - name: Sign and package
        run: |
          LDID=$(find /tmp/cctools -name "ldid" -type f | head -1)
          [ -n "$LDID" ] && $LDID -S JYJH_AntiDetect.dylib
          
          echo '{ Filter = { Bundles = ( "com.jyjh.whwb" ); }; }' > JYJH_AntiDetect.plist
          mkdir -p output
          cp JYJH_AntiDetect.dylib JYJH_AntiDetect.plist output/
          file output/JYJH_AntiDetect.dylib
          ls -lh output/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: JYJH-AntiDetect-dylib-v3.0
          path: output/

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: anti-v3.0
          files: output/*
          body: |
            ## 剑影江湖 反检测 v3.0 - MethodInfo指针替换
            - **v3.0核心**: 放弃DobbyHook，改用MethodInfo->methodPointer替换
            - **原因**: iOS 17 PPL阻止代码段修改，DobbyHook始终FAILED
            - **新方案**: 直接修改IL2CPP MethodInfo结构体中的methodPointer字段
              - 修改的是堆内存数据结构，不碰__TEXT代码段
              - PPL不管堆内存，所以不受限制
            - Hook ioctl: Cmd_IsRootDevice(2)→"0"
            - Hook getToken: 修改返回结果code=0
            - Hook MyGetToken.onResult: 确保回调code=0
            - Hook MyHtpCallback.onReceive: 强制code=0
            - Hook isHtpExist: 返回YES
            - 修改isInitialized静态字段为true
            - **无需Dobby库**，dylib更小更干净
            - **适用环境**: iOS 17.0 未越狱 + 巨魔商店
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
